import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  text: {
    margin: 0,
    fontSize: 40,
    textAlign: 'center',

    fontFamily: 'DM Sans',
    fontStyle: 'normal'
  },
  image: {
    width: '100%',
  }
});